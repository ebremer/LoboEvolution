<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>org.loboevolution</groupId>
    <artifactId>loboevolution-parent</artifactId>
    <version>1.0</version>
    <packaging>pom</packaging>
    <name>Lobo Evolution Parent</name>
    <description>
        Shared dependency-version management for all Lobo modules. Modules
        declare this as their &lt;parent&gt; and omit versions for the
        third-party libraries below, so a version lives in exactly one place and
        cannot drift between modules. This is NOT the reactor aggregator (that is
        the root pom.xml, which also builds the uber jar); it carries management
        only and deliberately has no &lt;dependencies&gt; or build plugins, so
        children inherit nothing but the managed versions.
    </description>

    <properties>
        <maven.compiler.source>25</maven.compiler.source>
        <maven.compiler.target>25</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <lombok.version>1.18.42</lombok.version>
        <logback.version>1.5.32</logback.version>
        <slf4j.version>2.0.17</slf4j.version>
        <json.version>20240303</json.version>
        <sqlite.version>3.46.0.0</sqlite.version>
        <rsyntaxtextarea.version>3.5.1</rsyntaxtextarea.version>
        <glassfish-corba.version>4.2.5</glassfish-corba.version>
        <junit.version>5.11.0</junit.version>
        <junit-platform.version>1.11.0</junit-platform.version>
        <!--
            GraalJS/Truffle MUST match the GraalVM JDK it runs on. Truffle binds
            to the JDK's in-process libgraal via JNI, and a patch-level mismatch
            (e.g. Truffle 25.0.0 on a GraalVM 25.1.x runtime) throws
            UnsatisfiedLinkError (LibGraalScope.attachThreadTo) the first time a
            script is evaluated, taking down the whole JS engine. Keep this in
            lock-step with the target GraalVM release.
        -->
        <graaljs.version>25.1.3</graaljs.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.projectlombok</groupId>
                <artifactId>lombok</artifactId>
                <version>${lombok.version}</version>
            </dependency>
            <dependency>
                <groupId>ch.qos.logback</groupId>
                <artifactId>logback-classic</artifactId>
                <version>${logback.version}</version>
            </dependency>
            <dependency>
                <groupId>org.slf4j</groupId>
                <artifactId>slf4j-api</artifactId>
                <version>${slf4j.version}</version>
            </dependency>
            <dependency>
                <groupId>org.json</groupId>
                <artifactId>json</artifactId>
                <version>${json.version}</version>
            </dependency>
            <dependency>
                <groupId>org.graalvm.polyglot</groupId>
                <artifactId>polyglot</artifactId>
                <version>${graaljs.version}</version>
            </dependency>
            <dependency>
                <groupId>org.graalvm.polyglot</groupId>
                <artifactId>js-community</artifactId>
                <version>${graaljs.version}</version>
                <type>pom</type>
            </dependency>
            <dependency>
                <groupId>org.xerial</groupId>
                <artifactId>sqlite-jdbc</artifactId>
                <version>${sqlite.version}</version>
            </dependency>
            <dependency>
                <groupId>com.fifesoft</groupId>
                <artifactId>rsyntaxtextarea</artifactId>
                <version>${rsyntaxtextarea.version}</version>
            </dependency>
            <dependency>
                <groupId>org.glassfish.corba</groupId>
                <artifactId>glassfish-corba-omgapi</artifactId>
                <version>${glassfish-corba.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-engine</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-api</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.jupiter</groupId>
                <artifactId>junit-jupiter-params</artifactId>
                <version>${junit.version}</version>
            </dependency>
            <dependency>
                <groupId>org.junit.platform</groupId>
                <artifactId>junit-platform-suite</artifactId>
                <version>${junit-platform.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
